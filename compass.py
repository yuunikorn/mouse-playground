import numpy
import string
import random
from matrices import *

#directionality, north = 0, east = 1, south = 2, west = 3

directionCounter = size * size * 4 #just to be safe
print directionCounter

#def currDirection():

    #if right turn
        #directionCounter = directionCounter + 1

    #if left turn
        #directionCounter = directionCounter - 1 

    #else straight, 0

    #directionVal = directionCounter % 4

    #switch directionVal
    #
    #if 0, direction = North
    #if 1, direction = East
    #if 2, direction = South
    #if 3, direction = West

    #return direction
